<body>
  <h2><%= Kwadwo Owusu %></h2>

  <h2>List of Places I'd like to Visit (Rating 1–10)</h2>

  <ul>
    <% places.forEach(function(place) { %>
      <li>
        <%= place.name %> : <%= place.rating %>
      </li>
    <% }); %>
  </ul>
</body>